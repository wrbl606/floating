name: floating
description: Picture in Picture mode management for Flutter.
version: 0.0.1
author: Marcin Wróblewski
homepage: https://gitlab.com/nicram606/floating

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: eu.wroblewscy.marcin.floating.floating
        pluginClass: FloatingPlugin
